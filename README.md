# English Analyzer API

## Collaboration test

Start the FastAPI server locally:

```powershell
uvicorn main:app --reload --port 8001
```

Then open <http://127.0.0.1:8001/> to verify the API is running.
